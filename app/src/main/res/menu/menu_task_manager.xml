<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_search"
        android:title="Tìm kiếm"
        android:icon="@drawable/ic_search"
        app:showAsAction="ifRoom|collapseActionView"
        app:actionViewClass="androidx.appcompat.widget.SearchView" />

    <item
        android:id="@+id/action_sort"
        android:title="Sắp xếp"
        android:icon="@drawable/ic_sort"
        app:showAsAction="never">

        <menu>
            <item
                android:id="@+id/sort_by_date"
                android:title="Theo ngày tạo" />
            <item
                android:id="@+id/sort_by_priority"
                android:title="Theo mức độ ưu tiên" />
            <item
                android:id="@+id/sort_by_status"
                android:title="Theo trạng thái" />
            <item
                android:id="@+id/sort_by_due_date"
                android:title="Theo hạn hoàn thành" />
        </menu>

    </item>

</menu>