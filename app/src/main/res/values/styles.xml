<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- AppTheme for activities with no ActionBar -->
    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- DatePicker Theme - Rõ ràng và dễ nhìn -->
    <style name="AppDatePickerTheme" parent="Theme.AppCompat.Light.Dialog">
        <!-- Màu accent cho các element được chọn -->
        <item name="colorAccent">#2196F3</item>

        <!-- Màu text chính - đen đậm -->
        <item name="android:textColorPrimary">#212121</item>

        <!-- Màu text phụ - xám đậm -->
        <item name="android:textColorSecondary">#424242</item>

        <!-- Màu control khi được activate -->
        <item name="android:colorControlActivated">#2196F3</item>

        <!-- Màu highlight -->
        <item name="android:colorControlHighlight">#E3F2FD</item>

        <!-- Background trắng -->
        <item name="android:windowBackground">#FFFFFF</item>

        <!-- Màu text selected date -->
        <item name="android:textColorPrimaryInverse">#FFFFFF</item>

        <!-- Màu background của selected date -->
        <item name="android:colorPrimary">#2196F3</item>

        <!-- Màu border -->
        <item name="android:colorControlNormal">#757575</item>
    </style>

    <!-- Alternative Material DatePicker Theme -->
    <style name="MaterialDatePickerTheme" parent="Theme.MaterialComponents.Light.Dialog">
        <item name="colorPrimary">#2196F3</item>
        <item name="colorSecondary">#03DAC6</item>
        <item name="colorSurface">#FFFFFF</item>
        <item name="colorOnSurface">#212121</item>
        <item name="android:windowBackground">#FFFFFF</item>
    </style>

    <!-- Custom Spinner Style -->
    <style name="AppSpinner" parent="Widget.AppCompat.Spinner">
        <item name="android:textColor">#212121</item>
        <item name="android:textSize">16sp</item>
        <item name="android:background">@drawable/spinner_background</item>
    </style>

    <!-- Spinner Item Styles -->
    <style name="SpinnerItem" parent="Widget.AppCompat.TextView.SpinnerItem">
        <item name="android:textColor">#212121</item>
        <item name="android:textSize">16sp</item>
        <item name="android:padding">12dp</item>
    </style>

    <style name="SpinnerDropDownItem" parent="Widget.AppCompat.DropDownItem.Spinner">
        <item name="android:textColor">#212121</item>
        <item name="android:textSize">16sp</item>
        <item name="android:background">#FFFFFF</item>
        <item name="android:padding">12dp</item>
    </style>
</resources>